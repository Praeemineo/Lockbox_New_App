{
  "name": "lockbox-ui",
  "version": "1.0.0",
  "description": "Lockbox SAPUI5 Application",
  "scripts": {
    "build": "ui5 build --clean-dest --dest dist",
    "start": "ui5 serve --port 8080 --open index.html"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0",
    "ui5-task-zipper": "^3.1.3",
    "@sap/ux-ui5-tooling": "^1.0.0"
  }
}
